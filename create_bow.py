import math
import numpy
def build_bow(code,k):
    a=0
    L2=0
    bow=[]
    for a in a<k:
        c=code.count(a)
        bow.append(c)
        L2=L2+(c*c)
    s=math.sqrt(s)
    c=c/s
    return c